//accessing the id of the button and then adding a click event to that button
$('#button-tag').click(function(){
	//once that button is clicked, invoke the logic inside of that function
	$('#p-tag').text("Changed To This")
});
